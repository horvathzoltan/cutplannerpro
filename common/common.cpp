// #include "common.h"

// ProfileCategory Common::categoryFromString(const QString& str) {
//     if (str == "RollerTube") return ProfileCategory::RollerTube;
//     if (str == "BottomBar") return ProfileCategory::BottomBar;
//     return ProfileCategory::Unknown; // ha van ilyen, vagy dobj hibát
// }

// QString Common::categoryToString(ProfileCategory category) {
//     if(category == ProfileCategory::RollerTube) return "RollerTube";
//     if(category == ProfileCategory::BottomBar)  return "BottomBar";
//     return "Unknown";
// }

// QString Common::badgeColorForCategory(ProfileCategory cat) {
//     if (cat == ProfileCategory::RollerTube) return "#345678"; // acélkék (mélyebb tónus)
//     if (cat == ProfileCategory::BottomBar) return "#506070"; // kékesszürke
//     return "#7f8c8d";     // semleges, tompa szürke (Unknown)
// }

